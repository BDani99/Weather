import SwiftUI

struct TesztView: View {
    
    var body: some View {
        NavigationView {
            
            Text("")
        }
    }
}

struct TesztView_Previews: PreviewProvider {
    static var previews: some View {
        TesztView()
    }
}
